apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: monica-yolo-client
        image: monica8531/yolo-client:v1.0.0
       
        ports:
        - containerPort: 5000 